# Shipit configuration for Wasmer
# This tells Shipit how to build the application

[build]
# PHP version
php = "8.4"

# Database: MySQL only, NO PostgreSQL
# Shipit auto-detects extensions, but we'll override in hooks
database = "mysql"

# Build hooks
[build.hooks]
pre-build = ".shipit/hooks/pre-build.sh"
