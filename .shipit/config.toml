# Shipit configuration to prevent PostgreSQL detection
[build]
# Explicitly tell Shipit to only use MySQL
database = "mysql"

# Don't auto-detect PostgreSQL
[detection]
ignore-extensions = ["pdo_pgsql", "pgsql"]

